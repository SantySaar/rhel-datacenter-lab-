---
# requirements.yml — RH294/AU374
# Instalar con: ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
    version: ">=1.5"
  - name: community.general
    version: ">=7.0"
  - name: community.mysql
    version: ">=3.0"
  - name: containers.podman
    version: ">=1.10"
